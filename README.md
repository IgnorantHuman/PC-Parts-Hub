# PC-Parts-Hub
The system uses ASP.NET Core Identity for authentication and role-based authorization, Entity Framework Core with SQL Server for persistent data storage, and Bootstrap and jQuery for a responsive and interactive interface.
